

/*
const Sequelize  = require("sequelize");
const db = require('./db');

const User = db.define('user_tasks', {

    id: {
        type: Sequelize.INTEGER,
        autoIncrement: true,
        primaryKey: true
    },

    task: Sequelize.STRING,
    date: Sequelize.STRING

});

module.exports = User;
*/